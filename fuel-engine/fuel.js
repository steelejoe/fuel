/*
 *  FUEL engine
 */

const config = require("config");

function main() {

    // load the config
    // if no package was passed - launch the config server
    // if a package was passed - act on it

}